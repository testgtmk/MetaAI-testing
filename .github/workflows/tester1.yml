name: Wkflow 2

on:
  repository_dispatch:
    types: [event-type-2]

jobs:
  job2:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - name: Run job 2
        run: echo "This is my india Workflow 2"
